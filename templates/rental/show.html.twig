{% extends 'base.html.twig' %}

{% block title %}Rental{% endblock %}

{% block body %}
    <h1>Rental</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rental.id }}</td>
            </tr>
            <tr>
                <th>PickUpDate</th>
                <td>{{ rental.pickUpDate ? rental.pickUpDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DropOffDate</th>
                <td>{{ rental.dropOffDate ? rental.dropOffDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rental_index') }}">back to list</a>

    <a href="{{ path('rental_edit', {'id': rental.id}) }}">edit</a>

    {{ include('rental/_delete_form.html.twig') }}
{% endblock %}
